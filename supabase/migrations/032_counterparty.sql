-- ============================================================
-- AD Pulse — поле Контрагент на транзакциях
-- ============================================================
-- Кто привёз (приход) / кто забрал (расход). Nullable — заполняется
-- только вручную по желанию, не обязательно.

ALTER TABLE material_transactions ADD COLUMN IF NOT EXISTS counterparty text;

COMMENT ON COLUMN material_transactions.counterparty IS
  'Название компании или человека: кто привёз (приход) / кто забрал (расход)';
